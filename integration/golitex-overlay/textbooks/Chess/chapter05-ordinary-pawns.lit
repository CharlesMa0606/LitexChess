# Chapter 5: 兵的方向、双步、斜吃与阻挡
# @litex-site-route /textbook/Chess/ordinary-pawns
# @litex-web-component litex-chess-textbook chapter=ordinary-pawns

"""
兵把移动与吃子分成不同几何：直走必须空，斜走必须有可吃目标；双步还要求初始横线和中间格为空。

学习目标：
- 区分兵单步、双步和普通吃子。
- 理解兵不能横走，也不能直向吃子。
- 解释为什么双步同时检查中间格与目标格。

方向由 side 决定
白兵 rank 增加，黑兵 rank 减少。形式化定义不靠中文方向词，而是把 side=±1 代入坐标等式。

双步不是把单步乘二
它只在起始横线可用，并且经过格、目标格都必须为空；中间有棋子时，单步和双步都会失败。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::pawn_single_move(1, 0, 1, 5, 2, 5, 3, 0)
by def $rules::pawn_double_move(1, 0, 0, 1, 5, 2, 5, 4, 0)
by def $rules::pawn_capture(1, -1, 1, 5, 4, 4, 5, 0)
