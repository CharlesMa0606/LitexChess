# Chapter 9: 闪击、闪将与双将
# @litex-site-route /textbook/Chess/discovered-double-check
# @litex-web-component litex-chess-textbook chapter=discovered-double-check

"""
一个棋子离开线路可以释放后方滑行子的攻击；若移动棋子本身也同时将军，就形成双将。

学习目标：
- 识别遮挡子、潜伏攻击子和被攻击目标。
- 区分普通将军、闪将与双将。
- 理解双将时为何只能移动王。

闪击是开放线路的通用结构
遮挡子移开后，后方车、象或后开始攻击目标；若目标是王，称为闪将。若目标是后等棋子，则是一般闪击。

双将来自两个同时存在的攻击者
常见情形是移动棋子直接将军，同时开放后方棋子的线路。由于一次着法通常无法同时吃掉或挡住两个攻击者，合法应对只能是王移到安全格。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::result_witness(1, 2, 1)
by def $rules::king_safe(1, 0)
