# Chapter 11: 单马、单象、双马与“不能强杀/死局”的区别
# @litex-site-route /textbook/Chess/material-dead-positions
# @litex-web-component litex-chess-textbook chapter=material-dead-positions

"""
从材料结构区分自动死局、理论上不能强制将杀以及仍可能在配合下出现将杀的局面。

学习目标：
- 理解王马对王与王象对王为何属于死局。
- 知道“双马通常不能强杀”不等于任何合法续着都不可能将死。
- 把死局、逼和与协议和棋区分开。

死局是对所有合法续着的断言
死局要求从当前局面出发，无论双方怎样合法走子，都不可能出现将死。因此它比“优势方无法强制取胜”更强。

单马与单象对单王
王加单马或王加单象无法同时覆盖形成将死所需的逃格和王所在格，因此保守识别器可以直接确认死局。

双马对单王为什么要谨慎
双马对单王通常不能在最佳防守下强制将杀，但若防守方配合，仍可能到达合法将杀图形。因此不能仅凭“不能强杀”把所有双马局面标成死局。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::admissible_position(1, 1, 0, 1)
by def $rules::result(0)
