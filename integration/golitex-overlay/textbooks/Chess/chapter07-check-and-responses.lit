# Chapter 7: 将军与三类应将：移王、吃子、挡线
# @litex-site-route /textbook/Chess/check-and-responses
# @litex-web-component litex-chess-textbook chapter=check-and-responses

"""
正在受将时，任何看似正常的棋子走法都必须真正解除攻击；局部几何成立不是充分条件。

学习目标：
- 从攻击者、路径和王格读出将军。
- 掌握移王、吃掉将军子、阻挡滑行攻击三类应对。
- 理解马将和贴身将通常不能通过挡线解决。

应将的统一判据是走后本方王安全
引擎不需要为‘应将’另写一个特殊开关。所有候选都在 candidate after 上重新扫描本方王；攻击者数量非零即拒绝。

挡将只对滑行攻击有效
车、象、后与王之间若存在严格中间格，可以把棋子放入攻击路径；马的攻击没有中间格，兵和王的邻接攻击也无法通过挡线解除。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::king_safe(1, 0)
by def $rules::result_witness(1, 1, 3)
