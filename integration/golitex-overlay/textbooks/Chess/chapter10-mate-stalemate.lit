# Chapter 10: 将死、逼和、死局与合法着集合
# @litex-site-route /textbook/Chess/mate-stalemate
# @litex-web-component litex-chess-textbook chapter=mate-stalemate

"""
终局状态不是看某一个格子，而是把当前是否受将与全部合法着是否为空结合起来。

学习目标：
- 用‘受将 + 零合法着’定义将死。
- 用‘未受将 + 零合法着’定义逼和。
- 区分逼和与死局：死局可能仍有合法着。

将死与逼和只差一个条件
两者都要求轮到行棋的一方没有任何合法着。若王在受将则是将死；若王不受将则是逼和并立即和棋。

死局关注未来是否可能出现将死
FIDE 的死局概念要求任何合法着序列都不可能导致将死。仅剩双王、王象对王、王马对王是直接例子。当前演示器只保守识别这些标准材料情形。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::result_witness(2, 1, 0)
by def $rules::result_witness(3, 0, 0)
