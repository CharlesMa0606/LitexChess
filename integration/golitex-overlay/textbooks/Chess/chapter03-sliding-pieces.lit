# Chapter 3: 车、象与后：射线、阻挡和目标占用
# @litex-site-route /textbook/Chess/sliding-pieces
# @litex-web-component litex-chess-textbook chapter=sliding-pieces

"""
滑行棋子的规则由几何方向、路径清空和目标归属共同组成；不能只看起点终点形状。

学习目标：
- 计算车线、象线与后复合几何。
- 区分中间阻挡与目标格己方棋子。
- 从路径列表定位第一枚阻挡子。

路径只包含严格位于起终点之间的格子
目标格是否可落由目标占用规则处理，中间格是否全空由 path_clear 处理。两者不能混为一谈。

后不是任意长距离棋子
后的允许几何是车线或象线的并集。坐标差 (1,2) 即使路径看起来短，也不是后步。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::rook_move(4, 0, 1, 1, 1, 1, 4, 0, 0)
by def $rules::bishop_move(3, 0, 1, 3, 1, 6, 4, 0, 0)
by def $rules::queen_move(5, 0, 1, 4, 1, 8, 5, 0, 0)
