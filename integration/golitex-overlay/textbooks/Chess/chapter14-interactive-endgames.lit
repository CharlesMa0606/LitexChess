# Chapter 14: 互动基础残局训练
# @litex-site-route /textbook/Chess/interactive-endgames
# @litex-web-component litex-chess-textbook chapter=interactive-endgames

"""
在教材迷你棋盘中选择源格与目标格；学习者着法和自动防守着都复用工作台的同一 Litex 门禁。

学习目标：
- 完成王车对王训练
- 完成王后对王训练
- 观察王马对王为何属于自动死局
- 阅读合法目标、提示、历史与状态

选择课程并读取训练目标
先辨认当前材料、行棋方和目标：王车、王后残局需要完成将杀；王马对王用于识别规则意义上的死局。

每一手都使用正式门禁
点击源格与目标格只构造候选。学习者着法和自动防守着都必须由同一 Litex 证书接受，失败时局面回滚。

训练反馈不是第二套棋规
“缩小盒子、己王靠近、避免逼和”等属于教学提示；合法性、将军、将死和逼和仍由统一状态链路给出。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::move(0, 8)
by def $rules::result(0)
