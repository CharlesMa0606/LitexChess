# Chapter 8: 绝对牵制与相对牵制：规则和战术的边界
# @litex-site-route /textbook/Chess/pins
# @litex-web-component litex-chess-textbook chapter=pins

"""
绝对牵制连接王安全，因此移动被牵制子可能非法；相对牵制只暴露后、车等棋子，通常仍是合法着。

学习目标：
- 区分绝对牵制与相对牵制。
- 解释为什么同样的开放线路在一个局面非法、另一个局面合法。
- 理解规则引擎不替玩家判断战术好坏。

绝对牵制是合法性问题
若遮挡子移开会让己方王直接受攻击，走后王安全失败；沿牵制线继续遮挡或吃掉攻击者则可能合法。

相对牵制是局面评价问题
若遮挡子后面是后或车而不是王，移开后即使损失重大，规则上仍可走。教材会把新打开的攻击显示出来，但 Litex 不应把‘坏棋’误判为‘非法棋’。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::moving_piece(4, 1)
by def $rules::king_safe(1, 0)
