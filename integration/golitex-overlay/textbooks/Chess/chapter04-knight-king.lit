# Chapter 4: 马与王：跳跃、相邻格与受攻击目标
# @litex-site-route /textbook/Chess/knight-king
# @litex-web-component litex-chess-textbook chapter=knight-king

"""
马没有滑行路径；王虽然只走一格，却必须额外满足走后目标不受攻击。

学习目标：
- 用坐标差描述马步。
- 理解‘马可越子’不等于‘可落在己方棋子上’。
- 把王的一步几何与王安全证书分开。

马只看起点、终点和目标归属
马的中间不存在经过格，因此周围棋子不会阻挡它；但目标格若有己方棋子仍然非法。

王步是局部几何加全局攻击关系
king_step 只说明坐标相邻。是否走入车、象、后、马、兵或敌王的攻击范围，由走后王安全阶段拒绝。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::knight_move(2, 0, 1, 7, 1, 6, 3, 0)
by def $rules::king_step(6, 0, 1, 5, 1, 5, 2, 0)
by def $rules::king_safe(1, 0)
