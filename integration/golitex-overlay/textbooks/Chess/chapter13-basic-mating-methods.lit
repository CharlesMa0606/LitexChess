# Chapter 13: 王车与王后杀王：限制、靠王与最后一将
# @litex-site-route /textbook/Chess/basic-mating-methods
# @litex-web-component litex-chess-textbook chapter=basic-mating-methods

"""
从“建立盒子—己王靠近—继续压缩—边线收官”的顺序学习基础重子残局，并明确避免逼和。

学习目标：
- 用车或后逐步缩小敌王活动区。
- 理解己王为何必须靠近并控制逃格。
- 在最后一将前持续检查重子安全与逼和风险。

第一阶段：建立并缩小盒子
用重子切断一条横线、纵线或对角线，把敌王限制到更小区域。不要把重子放到敌王可直接吃到且无人保护的位置。

第二阶段：让己王靠近
重子只能限制部分逃格，最后的相邻逃格需要己王控制。王之间不能相邻，因此要按“对王”关系推进。

第三阶段：受保护的最后一将
敌王到边线后，由己王控制相邻逃格，重子在安全距离上将军。若没有将军却封死全部合法格，结果是逼和。

本文件是 Litex 原生教材的可验证回退层。网页上的棋盘、局面实验、源码查看器和残局训练
由无 iframe 的 Chess Web Component 挂载到同一个教材路由中；下面的语句仍可由 Litex
项目运行器独立检查，因此即使交互扩展未加载，本章也不是一张纯静态网页。
"""

# 本章的最小可执行检查。完整局面证书由交互扩展的后端按每一手棋生成。
by def $rules::rook_move(4, 0, 1, 1, 3, 1, 7, 0, 0)
by def $rules::queen_move(5, 0, 1, 4, 4, 8, 8, 0, 0)
by def $rules::king_step(6, 0, 1, 5, 5, 6, 5, 0)
