# Research-only formal blueprints

These files are design studies and relational roadmaps. They are not loaded by
`Litex Chess Studio` at runtime and are deliberately separated from the
production kernel in `formal/chess_rules.lit`.
